#include <iostream>
#include <fstream>
#include "graph/Graph.h"


int main() {
    Graph mygraph = Graph();
    mygraph.sequential();
   //mygraph.largestDegree();

    /*graph a = graph(10);
    a.addEdge(12, 15);
//    a.addEdge(1,2);
    a.addEdge(2,3);
    a.addEdge(3,4);*/
    return 0;
}
